export interface TabInfo {
    heading: string;
    active?: boolean;
    right?: boolean;
}

export interface TabData {
    id: number;
    heading: string;
    active: boolean;
    right: boolean;
    fullscreen: boolean;
}
